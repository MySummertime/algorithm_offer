using stack to reverse list, with recursive algorithm

    1. recursive end condition: head == nullptr;
    2. recursive formula: head->next;
    3. return value: head->val;
    
class Solution{
public:
    vector<int> reversePrint(ListNode* head){
        func(head);
        return ans;
    }

private:
    void func(ListNode* head){
        if(head == nullptr){
            return;
        }
        func(head->next);
        ans.push_back(head->val);
    }
    
private:
    vector<int> ans;

}
